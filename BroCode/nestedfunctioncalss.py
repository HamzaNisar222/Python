# nested function calls means calling functions within a function the inner most is resolved first and used as an argument for the outer function 

print(round(abs(float(input("Enter a whole positive number?:")))))